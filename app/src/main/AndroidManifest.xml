<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Afitech"
        tools:targetApi="31">

        <!-- Déclaration de l'activité principale (splash screen) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Déclaration de l'activité d'accueil -->
        <activity
            android:name=".login"
            android:exported="true">
        </activity>
        <activity
            android:name=".Acceuil"
            android:exported="true">
        </activity>
        <activity
            android:name=".ClothingActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".Commande"
            android:exported="true">
        </activity>
        <activity
            android:name=".Commantaire"
            android:exported="true">
        </activity>
        <activity
            android:name=".Parrainage"
            android:exported="true">
        </activity>

        <activity
            android:name=".Commission"
            android:exported="true">
        </activity>



    </application>

    <!-- Ajoutez les permissions nécessaires ici, par exemple : -->
    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->

</manifest>
